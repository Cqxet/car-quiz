# Araba Testi

Vercel `npm run build` calistirir. Next ESLint hatalarini derleme hatasi sayar; `prefer-const` yayini dusuruyordu.
Simdi `eslint.ignoreDuringBuilds` acik ve `build` = `next build` (webpack).
